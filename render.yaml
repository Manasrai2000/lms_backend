services:
  - type: web
    name: lms-backend
    env: node
    region: singapore
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVarGroups: []
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
